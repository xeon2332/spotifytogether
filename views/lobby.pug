html
    head
        title= spotifytogether
    body
        h1= msg
        
        script(src="/static/jquery-3.4.1.min.js")
        script(src="/static/checker.js" type="text/javascript")
        script.
            $(document).ready(function(){
                if(getcookie("host") == "1")
                    checkspotify()
            })